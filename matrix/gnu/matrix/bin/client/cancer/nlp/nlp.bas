/'
If regular point x∗ is a local solution of the NLP, then there exists a
λ∗ ∈ C satisfying the 1st order necessary optimality conditions:
∇x f (x∗) + cx (x∗)∗λ∗ = 0
c(x∗) = 0
'/
Declare Function NLP CDecl Alias "Axf(x) + cx(x)*A = 0" (ByRef order As Integer, ByVal optimality As Integer) As Integer


#include once "fbc-int/array.bi"

Dim As LongInt array(4, 5)
Dim As UInteger array_size

array_size = array(4,5)             

End
